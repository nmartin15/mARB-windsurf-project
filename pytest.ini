[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    unit: fast isolated unit tests
    integration: integration tests for loader/pipeline behavior
    db: database-backed tests requiring DATABASE_URL
